# Threshold
a private eligibility attestation app built on Midnight. A user proves they satisfy a numeric or categorical rule (a threshold, a membership check, a compliance condition) to a verifying party, without revealing the underlying private value.
